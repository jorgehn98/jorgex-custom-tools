{
  "name": "@jorgex/hooks",
  "version": "1.0.0",
  "description": "Generic hooks plugin - runs scripts on session/tool events from project and global config",
  "type": "module",
  "main": "src/hooks.ts",
  "scripts": {
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@opencode-ai/plugin": "1.3.2"
  }
}
